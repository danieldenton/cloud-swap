import React from "react";
interface Props {
    title: string;
    transactionHash: string;
    setShowAlert: (arg0: boolean) => void;
    isAction: boolean;
    isSuccess: boolean;
    showAlert: boolean;
}
declare const Alert: ({ title, transactionHash, setShowAlert, isAction, isSuccess, showAlert, }: Props) => React.JSX.Element;
export default Alert;
//# sourceMappingURL=Alert.d.ts.map